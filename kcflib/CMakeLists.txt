AUX_SOURCE_DIRECTORY(. DIR_LIB_SRCS)

FIND_PACKAGE(OpenCV REQUIRED)

ADD_LIBRARY(kcflib STATIC ${DIR_LIB_SRCS})

SET(LIBRARY_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/lib)

TARGET_LINK_LIBRARIES(kcflib ${OpenCV_LIBS})



